import React, { useMemo } from "react";
import { FileText } from "lucide-react";
import { CriarMaterialSimple } from "./criar-material-simple";
import { InfoNote } from "./info-note";
import { useTranslations } from "../../i18n/use-translations";

export function DocumentosCorporativos() {
  const { tGroup, language } = useTranslations();
  const tools = useMemo(() => tGroup('tools'), [tGroup, language]);
  
  return (
    <div className="p-6 max-w-[1400px] mx-auto">
      <div className="space-y-6">
        <div className="space-y-2">
          <div className="flex items-center gap-3">
            <div className="p-2 rounded-lg bg-primary/10">
              <FileText className="w-6 h-6 text-primary" />
            </div>
            <h1 className="text-3xl font-bold">{tools.corporateDocumentsTitle}</h1>
          </div>
          <p className="text-muted-foreground mt-1">
            {tools.corporateDocumentsDescription}
          </p>
        </div>

        {/* Info Note */}
        <InfoNote title={tools.documentsTip}>
          {tools.documentsAutoGenerated}
        </InfoNote>

        {/* Motor de Materiais */}
        <CriarMaterialSimple categoryFilter="documentos" />
      </div>
    </div>
  );
}